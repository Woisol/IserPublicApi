## 代码风格建议

1. 尽量复用项目已有的代码，避免重复造轮子，提高代码的一致性和可维护性。
  1. 不建议使用过多函数，过多的函数会增加代码的复杂度和维护成本，尤其是在函数的内容只是简单获取某个属性或者逻辑非常简单时你应当直接内嵌。如果需要实现一个逻辑和现有函数相似但有不同的新函数，优先考虑给现有函数添加可选参数和重载来尽可能复用现有逻辑，而不是创建全新的函数。
  2. 不建议且不应当定义过多类型，不应当为每个函数的返回值都定义一个全新类型。你应当用全局的视角定义良好的类型体系，在需要新的类型时，优先考虑是否能复用或通过组合/变换（例如 TS 的 Exclude、Partial 等）已有类型来表达，保持类型精简。在我的前端项目中，我一般会在一个地方统一定义所有类型，只有一些特定于文件的类型除外。
  3. 对于前端项目，优先使用项目已经在使用的组件库来实现逻辑样式。如果需要手写 CSS，请同样尽量尝试复用已有类名和样式。我一般会维护一套全项目适用的通用类名（例如按钮我通常会定义一套 btn-* 类名，全局使用）。对于边框、阴影、颜色等属性，除非确实只在这一处使用，否则禁止硬编码而应当使用全局维护的 CSS 变量体系。
2. 除非项目非常简单，否则不要扁平地写代码文件。即使是在项目的一开始也应当规划好合理的目录结构和文件结构。如果你不确定如何规划，请向我请示。
3. 对于测试，大部分功能的新增/修改都应当补充、修正相关的测试用例。但不要为了测试而测试，一些诸如添加注释、修改样式等不涉及核心逻辑的修改不要添加莫名其妙的测试。

## 风险控制
1. 永远不要在代码中写入任何敏感信息，例如 API 密钥、数据库连接字符串等。这些信息应该通过环境变量或安全的配置管理工具来管理。

## 个人需求
1. 除非要求，否则应当原样保留代码中已有的注释，它们一般是我学习的标注。
2. 同样，你添加的注释也应当默认使用中文

## 流程控制
1. 除非明确要求，否则禁止在本轮对话中“顺手”修改我没有明确的修改点
2. 如果你在过程中发现多次读取的代码不同，一般是我自己的手修，忽视并继续主任务即可。
3. 对项目进行重构时，除非要求否则不应保留旧代码/类名。有 git 兜底可以大胆删去，防止冲突。

## 环境相关
1. 代码中常包含中文文件，请你在读写时使用 utf-8 编码。
